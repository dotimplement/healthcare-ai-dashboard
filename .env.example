# GitHub personal access token, used by refresh.py and healthcare_ai/*.
# Create one at https://github.com/settings/tokens -- public_repo scope is enough.
# Unauthenticated requests are capped at 60/hour, far too few to classify 385 owners.
GITHUB_TOKEN=ghp_replace_me
